@model IEnumerable<Category>
<div>
    <ul class="list-group my-4">
        @foreach(var cat in Model)
        {
            <li class="list-group-item">
                @cat.CategoryName
            </li>
        }
    </ul>
</div>